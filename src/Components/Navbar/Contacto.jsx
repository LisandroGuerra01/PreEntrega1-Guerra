import {AiOutlineMail} from 'react-icons/ai';

const Contacto = () => {
    return (
        <div>
            <button><AiOutlineMail /> Contacto</button>
        </div>
    );
};

export default Contacto;